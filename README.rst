nailbiter - a storage-backend-agnostic thumbnail generator
----------------------------------------------------------

``nailbiter`` is a simple Django thumbnail generation field,
modeled after ``sorl-thumbnail``. 